# Design output

Empty until the first round lands.

One directory per round, numbered, never overwritten:

- `01-directions/` — three competing directions, each on the same three screens
- `02-screens/` — the chosen direction, taken through the whole product
- `03-.../` — whatever the polishing needs

Each with a `README.md` saying what was decided and what was given up.
